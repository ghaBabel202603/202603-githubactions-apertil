name: 04 - Uso de Actions
on:
  push:
jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest
    steps:
    - name: "Checkout Code"
      uses: actions/checkout@v4 

    - name: "Printing Folders"
      run: |
        echo ls -la